from django.conf.urls import include, url
#from People.views import *
from . import views
urlpatterns = [
    url(r'^result/', views.result),
    url(r'^index/', views.index),
    url(r'^$', views.realview),
]
